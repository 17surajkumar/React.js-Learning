export { TodoContext, TodoProvider, useTodo } from "./TodoContext";

// keep all the imported file and function in one single file then you have to only import to one single file to your App.jsx and you are ready to go and use all the other funcionality
